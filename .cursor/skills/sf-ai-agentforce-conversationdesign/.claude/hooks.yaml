# Claude Code lifecycle hooks for sf-ai-agentforce-conversationdesign
# These hooks are registered by the installer (tools/install.py)
# and are NOT part of the Agent Skills open specification.

PostToolUse:
- matcher: "Write|Edit"
  hooks:
  - type: command
    command: "python3 ${SKILL_HOOKS}/instruction-quality-validator.py"
    timeout: 10000
